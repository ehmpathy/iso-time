# 2.detect.hazards — identified

## test hazards

### 1. slowtest reporter added (medium risk)
- **what**: jest configs now include `test-fns/slowtest.reporter.jest` reporter
- **why may break**: reporter must be exported from `test-fns@1.15.7`; if not found, jest fails on startup
- **verify**: `npm run test:unit -- --listTests`

### 2. keyrack replaces apikeys (high risk)
- **what**: `jest.integration.env.ts` and `jest.acceptance.env.ts` now use `keyrack.source()` instead of reading from `.agent/repo=.this/role=any/skills/use.apikeys.json`
- **why may break**: keyrack must be unlocked and configured; tests will fail if keyrack not set up
- **verify**: `npm run test:integration`

### 3. jest cli flag renamed (medium risk)
- **what**: workflow changed `--testPathPattern` to `--testPathPatterns` (plural)
- **why may break**: flag name depends on jest version; if unsupported, ci will fail
- **verify**: check jest@30.2.0 docs or run `npm run test:integration -- --help`

## config hazards

### 4. tsconfig module changed (high risk)
- **what**: `tsconfig.json` changed `module: "commonjs"` → `"node16"` and `moduleResolution: "node"` → `"node16"`
- **why may break**:
  - imports may be found via different paths
  - requires `node:` prefix for node builtins (already updated in jest env files)
  - may affect how `.js` extensions are located
- **verify**: `npm run test:types`

### 5. biome rule added (low risk)
- **what**: `noThisInStatic` rule added as warn (no autofix)
- **why may break**: will emit new lint warns but not fail build
- **verify**: `npm run test:lint`

## cicd hazards

### 6. prepare:rhachet added to ci (low risk)
- **what**: github workflow now runs `npm run prepare:rhachet --if-present` before integration/acceptance tests
- **why may break**: requires rhachet init to succeed; creates symlinks for keyrack
- **verify**: ci will show if this step fails

### 7. test:lint:cycles added (medium risk)
- **what**: `npm run test:lint` now includes `test:lint:cycles` which runs `dpdm` to detect circular deps
- **why may break**: if circular deps exist in codebase, lint will fail
- **verify**: `npm run test:lint:cycles`

### 8. branch protection context added (low risk)
- **what**: `provision/github.repo/resources.ts` added `'suite / enshard'` to required ci contexts
- **why may break**: if enshard job not present in workflow, prs cannot merge
- **verify**: confirmed — enshard job exists in `.github/workflows/.test.yml`

### 9. github app owner changed to dynamic (low risk)
- **what**: `provision.yml` and `release.yml` changed `owner: ehmpathy` to `${{ github.repository_owner }}`
- **why may break**: if repo is forked, owner will differ — but this is intentional for portability
- **verify**: works correctly for ehmpathy org repos

## path hazards

### 8. apikeys files deleted (info)
- **what**: `.agent/repo=.this/role=any/skills/use.apikeys.json` and `use.apikeys.sh` deleted
- **why may break**: any code or scripts that source these files will fail
- **verify**: grep for `use.apikeys` in codebase

## recommended verification order

1. `npm run test:types` — catch tsconfig issues
2. `npm run test:lint` — catch biome/cycles issues
3. `npm run test:unit` — catch reporter/import issues
4. `npm run test:integration` — catch keyrack/api issues
